import type { Metadata } from 'next';
export const metadata:Metadata={title:'The living record · Unwatched',description:'Follow real attempts, shared knowledge and changes made by the people of Unwatched.'};
export default function Layout({children}:{children:React.ReactNode}){return children;}
